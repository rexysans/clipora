# Worker Environment Variables

# Environment
NODE_ENV=development
LOG_LEVEL=INFO

# Database - Option 1: Use DATABASE_URL (Production)
# DATABASE_URL=postgresql://user:password@host:5432/database
# DB_SSL=true

# Database - Option 2: Individual credentials (Development)
DB_USER=stream_app
DB_HOST=127.0.0.1
DB_NAME=stream_platform
DB_PASSWORD=streampass
DB_PORT=5432

# Storage Paths (absolute paths)
UPLOAD_DIR=/path/to/backend/uploads
HLS_DIR=/path/to/videos/hls
THUMB_DIR=/path/to/videos/thumbs
